node_modules
dist
# SSR bundle for the build-time prerender; consumed by scripts/prerender-meta.mjs
# and never deployed.
dist-ssr
.env
.env.local
.DS_Store
*.local

# Large video files — exceed GitHub's 100 MB limit
# Compressed (_cmp) versions are tracked; originals are not
*.mp4
*.mov
*.webm
!*_cmp.mp4
!*_cmp.mov
!*_cmp.webm

# Local editor/tool config (tool resolves this from the session cwd, not here)
.claude/launch.json

# Working notes: outreach copy, Search Console steps. Kept locally, not
# published -- this repo is public and draft self-promotion copy sitting next
# to the engineering work reads oddly to anyone browsing it.
notes/
