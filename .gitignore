node_modules
dist
.env
.env.local
.DS_Store
*.local

# Large video files — exceed GitHub's 100 MB limit
# Compressed (_cmp) versions are tracked; originals are not
*.mp4
*.mov
*.webm
!*_cmp.mp4
!*_cmp.mov
!*_cmp.webm
